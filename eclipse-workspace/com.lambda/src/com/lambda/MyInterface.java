package com.lambda;

// you can add annotation so that it can tell functional interface or just can have only one abstract method, it takes as functional interface.

//@FunctionalInterface
public interface MyInterface {
	
	public abstract void hello();

}
